# 앱 서비스 경로: 헤더 검증 적용
location ^~ /app/ {
    access_by_lua_file /src/domain/auth/handler/AuthHandler.lua;
    proxy_pass http://127.0.0.1:3000;
}

# 웹 서비스 경로: 헤더 검증 없이 전달
location ^~ /web/ {
    proxy_pass http://127.0.0.1:4000;
}
